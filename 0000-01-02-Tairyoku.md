function finder(num) {
  if ( /\b1/.test(num) || /\b2/.test(num) || /\b5/.test(num) )
  return num;
}
let arr = [10, 20, 30, 50, 235, 3000];
let newarr = [];
for ( let num of arr) {
 if (finder(num))
newarr.push(finder(num));
}
alert(newarr);
