function finder(num) {
  if ( String(num).startsWith('1') || String(num).startsWith('2') || String(num).startsWith('5') )
  return num;
}
let arr = [10, 20, 30, 50, 235, 3000];
let newarr = [];
for ( let num of arr) {
 if (finder(num))
newarr.push(num);
}
alert(newarr);
